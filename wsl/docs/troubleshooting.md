# Troubleshooting

## WSL says "Theme not found"

- Run `wsl/diagnostics/wsl-theme-check.sh`.
- Confirm theme exists in `~/.poshthemes`.
- Confirm symlink target matches Windows theme directory.
- Confirm `.poshtheme` contains a valid theme name.
